# Mapa de Clientes — Deploy

Este folder está listo para publicarse en:

- **GitHub Pages** (en la rama `gh-pages` o via `docs/`)
- **Netlify** (arrastrar y soltar)
- **Vercel** (importar repo)
- **Azure Static Web Apps** (opcional)

Contenido:
- `index.html`  ← mapa interactivo listo.

